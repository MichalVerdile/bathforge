[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "npx serve -s build -l 3000"

[phases.install]
dependsOn = ["setup"]
cmds = ["npm install -g serve"]
